## [claude - joaumvictor.oficial@gmail.com] 06/06/2026 — sessão longa

**Comando:** /dev
**Complexidade:** 🔴 Grande
**Arquivos tocados:**
- docker-compose.yml (modificado)
- PLANO.md (criado)
- public/assets/heroes/hero/ (criado — walk-1..6, attack-1..3, idle-1, death-1)
- public/assets/creatures/zombie/ (criado — walk-1, idle-1, attack-1, death-1)
- src/game/renderer/sprites.ts (modificado — loadImageOptional, probeFrames)
- src/lib/spriteLoader.ts (criado — SpriteSet, loadSpriteSet, fallbacks)
- src/game/entities/Player.ts (modificado — usa loadSpriteSet)
- src/game/entities/Enemy.ts (modificado — usa loadSpriteSet, walk animation)
- src/game/Game.ts (modificado — simplificado start())
- src/app/api/admin/sprite-sets/route.ts (criado — filesystem scanner)
- prisma/schema.prisma (modificado — Player, HeroInstance, campos novos em Hero/Monster)
- src/app/api/auth/login/route.ts (criado — upsert por email)
- src/lib/session.ts (criado — getPlayerId, saveSession, etc)
- src/app/page.tsx (reescrito — login por email)
- src/components/GameCanvas.tsx (modificado — usa session.ts)
- src/app/api/progress/route.ts (modificado — suporta player e session)
- src/app/api/heroes/route.ts (criado — GET com flag owned)
- src/app/api/heroes/buy/route.ts (criado — POST compra com transaction)
- src/app/api/heroes/group/route.ts (criado — PUT posição no grupo, max 3)
- src/app/heroes/page.tsx (criado — UI de heróis com formação e compra)
- src/app/admin/page.tsx (modificado — admin de heróis/monstros/rounds completo)
- prisma/seed.ts (modificado — usa @prisma/adapter-pg, semeia Guerreiro/Arqueiro/Zumbi)

**O que foi feito:** Implementados os 3 checkpoints do CURRENT_TASK.md — convenção de sprites com filesystem discovery, auth por email (upsert no PostgreSQL), e sistema de compra de heróis com formação de grupo (máx 3). Admin panel completo com MonstersTab atualizada para novos campos (spriteSet, monsterType, isBoss, sizeMultiplier, attackBehavior, attackRange), SelectInput primitive adicionada, Pill com cores extra, duplicata de `pct` removida, erro de tipo em heroes/page.tsx corrigido.

**Critério de aceite:** ✅ TypeScript passa sem erros (`npx tsc --noEmit`). Sprite sets carregados via filesystem. Login cria Player por email. Heróis compráveis com moedas, grupo limitado a 3. Admin modela heróis, monstros e rounds.
**Status:** ✅ Concluído

---

## [claude - joaumvictor.oficial@gmail.com] 06/06/2026 — debug

**Comando:** /debug
**Erro:** `PrismaClientConstructorValidationError: Using engine type "client" requires either "adapter" or "accelerateUrl"`
**Causa raiz:** `src/lib/db.ts` criava `PrismaClient` sem adapter. Prisma 7 com `engineType = "client"` obriga o adapter no construtor — o seed.ts já foi corrigido antes, mas o db.ts central foi esquecido.
**Hipóteses testadas:** (1) db.ts sem adapter → ✅ confirmada imediatamente pelo stack trace
**Arquivos tocados:**
- src/lib/db.ts (modificado — adicionado Pool + PrismaPg adapter, igual ao seed.ts)
**Fix aplicado:** Envolto em `createClient()` que instancia `Pool` e `PrismaPg` antes de criar `PrismaClient({ adapter })`.
**Status:** ✅ Resolvido

---

## [claude - joaumvictor.oficial@gmail.com] 06/06/2026 — FASE 2

**Comando:** /dev
**Complexidade:** 🔴 Grande
**Arquivos tocados:**
- prisma/schema.prisma (modificado — baseXp adicionado ao Monster)
- prisma/seed.ts (modificado — baseXp: 8 no Zumbi)
- src/app/api/combat/kill/route.ts (criado — distribui XP ao grupo, lida com level up)
- src/game/entities/Enemy.ts (modificado — campo baseXp no construtor)
- src/game/Game.ts (modificado — onKill callback, addFloat público, setHeroInfo, HUD com badges de nível)
- src/components/GameCanvas.tsx (modificado — conecta onKill ao endpoint, carrega grupo ao init)
- src/app/heroes/page.tsx (modificado — xpToNext helper, barra de XP, stats escalados por nível)
- src/app/admin/page.tsx (modificado — campo baseXp no form e tipo Monster)

**O que foi feito:** Sistema completo de XP e level up. Monstro morre → Game.ts dispara onKill → GameCanvas chama /api/combat/kill → XP distribuído entre heróis do grupo → level up automático quando xp >= xpToNext (100 * lv^1.5) → floats "+X XP" e "HERO LV.N!" aparecem no canvas → badges de nível com mini barra de XP no HUD → página /heroes mostra barra de XP e stats escalados por nível.

**Critério de aceite:** ✅ TypeScript passa sem erros. Schema atualizado e client regenerado. Floats de XP e level up integrados ao Game. HUD mostra Lv. de cada herói do grupo. /heroes exibe barra de XP e stats escalados.
**Status:** ✅ Concluído

---

## [claude - joaumvictor.oficial@gmail.com] 06/06/2026 — FASE 2 drops

**Comando:** /dev
**Complexidade:** 🔴 Grande
**Arquivos tocados:**
- prisma/schema.prisma (modificado — Item expandido, novos models LootEntry e InventoryItem, relações em Player e Monster)
- prisma/seed.ts (modificado — 5 itens base + 5 LootEntries no Zumbi)
- src/game/entities/Enemy.ts (modificado — campo monsterId opcional no construtor)
- src/game/Game.ts (modificado — monsterId passado para Enemy, onKill inclui monsterId)
- src/app/api/combat/kill/route.ts (modificado — inclui loot roll, cria InventoryItem, retorna drops[])
- src/components/GameCanvas.tsx (modificado — RARITY_COLORS, floats coloridos por raridade, passa monsterId ao endpoint)
- src/app/api/inventory/route.ts (criado — GET inventário com item incluído)
- src/components/InventoryPanel.tsx (reescrito — grid de itens reais, painel de detalhe, raridade colorida)
- src/app/heroes/page.tsx (modificado — link para mochila no header)

**O que foi feito:** Loop completo de loot: monstro morre → kill endpoint rola cada LootEntry (dropChance 0–1) → InventoryItem criado/incrementado no banco → float colorido aparece no canvas (cinza/verde/azul/dourado por raridade) → InventoryPanel mostra itens reais com grid clicável e detalhe de stats. 5 itens seedados (espada, elmo, bota, anel, fragmento de runa) com 5 loot entries no Zumbi.

**Critério de aceite:** ✅ TypeScript passa. Itens droparão de zumbis com probabilidade configurada. Float colorido por raridade aparece no canvas. Mochila no /play mostra itens reais com detalhe ao clicar.
**Status:** ✅ Concluído

---

## [claude - joaumvictor.oficial@gmail.com] 06/06/2026 — FASE 2 equipamentos

**Comando:** /dev
**Complexidade:** 🔴 Grande
**Arquivos tocados:**
- prisma/schema.prisma (modificado — model Equipment, relação em HeroInstance e InventoryItem)
- src/lib/heroStats.ts (criado — computeStats: base + nível + equipment)
- src/app/api/heroes/equip/route.ts (criado — POST equip + DELETE unequip com validação)
- src/app/api/heroes/route.ts (modificado — inclui equipment no retorno de instances)
- src/app/heroes/page.tsx (reescrito — slots de equipamento, picker de itens, stats calculados)
- src/game/entities/Player.ts (modificado — attack, speed, cooldownMs como parâmetros)
- src/game/Game.ts (modificado — aceita heroStats no construtor, usa player.attackDamage)
- src/components/GameCanvas.tsx (modificado — carrega stats com equipment ao init)

**O que foi feito:** Sistema completo de equipamentos. Herói mostra 6 slots (HEAD/MAINHAND/ARMOR/BOOTS/RING/OFFHAND); clicar slot vazio abre picker de itens compatíveis do inventário; clicar slot cheio desequipa. Stats exibidos usam computeStats (base + escala de nível + bônus de cada item). No canvas, herói é criado com HP e ATK reais do herói na posição Frente do grupo.

**Critério de aceite:** ✅ TypeScript passa. Equipar espada aumenta ATK exibido. Stats do jogo refletem herói equipado na frente do grupo.
**Status:** ✅ Concluído

---

## [claude - Victor Fausto] 06/06/2026 — FASE 3 mapa

**Comando:** /dev
**Complexidade:** 🔴 Grande
**Arquivos tocados:**
- prisma/schema.prisma (modificado — models World, Zone, Level, PlayerLevelProgress; back-relations em Player e Round)
- prisma/seed.ts (modificado — rounds com IDs fixos + waves + world/zone/3 levels)
- src/lib/session.ts (modificado — getSelectedLevel / setSelectedLevel)
- src/app/api/map/route.ts (criado — GET worlds com zones > levels > progress)
- src/app/api/levels/[id]/route.ts (criado — GET level info com zone e world)
- src/app/api/levels/[id]/waves/route.ts (criado — GET waves com WaveMonster > Monster)
- src/app/api/levels/[id]/complete/route.ts (criado — POST upsert PlayerLevelProgress)
- src/app/map/page.tsx (criado — tela de mapa com worlds/zones/levels, lock logic, estrelas)
- src/components/GameNavbar.tsx (modificado — link "MAPA" + MapIcon)
- src/game/Game.ts (modificado — WaveDefDB/WaveMonsterDB tipos exportados, construtor aceita waveDefs, onLevelComplete callback, setLevelName, playTimeMs, HUD dinâmico)
- src/components/GameCanvas.tsx (modificado — loadSelectedLevel, setLevelName, onLevelComplete com cálculo de estrelas e POST ao complete endpoint)

**O que foi feito:** Sistema completo de mapa data-driven. Hierarquia World > Zone > Level seedada ("Reino dos Mortos > Floresta Sombria > Fase 1/2/3") com rounds e waves no banco. Página /map mostra levels com estrelas, lock logic (fase anterior precisa ter stars > 0), botão "Jogar" salva levelId no localStorage. Game.ts carrega waves do banco via API ao iniciar, HUD mostra nome do level; ao vencer emite onLevelComplete → GameCanvas calcula estrelas (1=completou, 2=sem morrer, 3<90s) → POST /api/levels/[id]/complete → float "★ FASE COMPLETA!" no canvas.

**Critério de aceite:** ✅ TypeScript passa sem erros (`npx tsc --noEmit`). DB seedado com world/zone/levels verificado. /map renderiza Reino dos Mortos > Floresta Sombria > Fase 1/2/3. Clicar "Jogar" redireciona para /play com waves do banco. Vencer fase → estrelas salvas → próxima fase desbloqueada.
**Status:** ✅ Concluído

---

## [claude - Victor Fausto] 06/06/2026 — FASE 4 grupo completo no combate

**Comando:** /dev
**Complexidade:** 🔴 Grande
**Arquivos tocados:**
- src/game/entities/Enemy.ts (modificado — walkSpeed como parâmetro com default 42)
- src/game/Game.ts (reescrito — players: Player[], HeroSlot type, multi-hero autoMove/resolveCombat/tickEnemies, HUD per-hero com HP+XP bars, frontPlayer() helper, clampPlayers/calcCamera multi-herói, Game Over só com todos mortos)
- src/components/GameCanvas.tsx (reescrito — loadGroupData retorna HeroSlot[], new Game(canvas, coins, waveDefs, heroSlots), remove leadStats/heroInfo separados)

**O que foi feito:** Todos os heróis do grupo agora aparecem e combatem simultaneamente no canvas. Cada herói tem seu Player com stats individuais (HP, ATK, speed calculados por equipamentos via computeStats). Herói da frente (i=0) lidera; os outros mantêm formação escalonada 80px entre si. Monstros targetam o herói mais avançado vivo. Game Over apenas quando todos caem. HUD mostra badge por herói com HP bar (verde→amarelo→vermelho), XP fill e "MORTO" quando caído. Inimigos do banco respeitam walkSpeed configurado no admin.

**Critério de aceite:** ✅ TypeScript passa sem erros. 3 heróis no grupo → 3 sprites no canvas atacando. Herói morto fica cinza no HUD; grupo continua. Phase 'dead' só quando todos mortos. walkSpeed do banco aplicado aos inimigos.
**Status:** ✅ Concluído

---

## [claude - Victor Fausto] 06/06/2026 — FASE 9 mago com AOE

**Comando:** /dev
**Complexidade:** 🟡 Média
**Arquivos tocados:**
- prisma/seed.ts (modificado — hero-mage: Mago 300 moedas, HP 14, ATK 7, cooldown 2.8, range 2, heroClass='mage')
- src/game/Game.ts (modificado — SpellFlash interface + array; resolveCombat case 'mage' AOE hits all enemies in range + spawns SpellFlash; tickSpellFlashes; render anel roxo expansivo no bloco de câmera)

**O que foi feito:** Terceiro herói "Mago" (300 moedas) seedado e disponível para compra em /heroes. Quando o Mago ataca, um pulso arcano roxo expande de 20px até 200px (radius do attackRange) em 380ms com opacidade decrescente, atingindo TODOS os inimigos dentro do alcance simultaneamente. Guerreiro e Arqueiro mantêm comportamentos anteriores inalterados.

**Critério de aceite:** ✅ `npx tsc --noEmit` sem erros. Seed OK. Mago aparece em /heroes. AOE atinge múltiplos zumbis. Anel roxo expansivo visível no canvas.
**Status:** ✅ Concluído

---

## [claude - Victor Fausto] 06/06/2026 — FASE 8 projéteis visuais para heróis ranged

**Comando:** /dev
**Complexidade:** 🟡 Média
**Arquivos tocados:**
- src/game/entities/Projectile.ts (criado — orb laranja com trail, viaja a 520px/s, desativa ao colidir ou sair da tela)
- src/game/entities/Player.ts (modificado — `heroClass: string` readonly no construtor, default 'warrior')
- src/game/Game.ts (modificado — HeroSlot.heroClass; import Projectile; projectiles array; resolveCombat bifurca melee/ranged; spawnProjectile; tickProjectiles com colisão; render projéteis no espaço-mundo)
- src/components/GameCanvas.tsx (modificado — HeroApiItem.heroClass; heroClass passado no HeroSlot)

**O que foi feito:** Arqueiro agora dispara projéteis laranjas visíveis ao atacar. `resolveCombat()` verifica `p.heroClass === 'archer'` — se ranged, spawna um `Projectile` apontado ao inimigo mais próximo; se melee, dano instantâneo como antes. `tickProjectiles()` move os orbs frame a frame e testa colisão (hitbox = `enemy.width/2 + 10`). Projétil desativa ao colidir com inimigo ou ao sair do range máximo (900px). Guerreiro mantém comportamento original.

**Critério de aceite:** ✅ `npx tsc --noEmit` sem erros. Arqueiro dispara orb laranja com trail visível. Projétil some ao atingir zumbi. Guerreiro ataca instantaneamente sem projétil.
**Status:** ✅ Concluído

---

## [claude - Victor Fausto] 06/06/2026 — FASE 7 sistema de boss

**Comando:** /dev
**Complexidade:** 🟡 Média
**Arquivos tocados:**
- prisma/seed.ts (modificado — monster-zombie-king, round-boss-1, 3 waves, level-4 "Fase 4 — O Rei")
- src/game/entities/Enemy.ts (modificado — isBoss, sizeMultiplier, name como readonly; width/height dinâmicos no construtor; draw() com drawY ajustado para base no chão; HP bar vermelha escura para boss)
- src/game/Game.ts (modificado — WaveMonsterDB com isBoss/sizeMultiplier/name; Enemy recebe novos params; Announcement com color?; triggerWaves detecta boss e dispara anúncio vermelho; activeBoss() helper; boss HP bar gradiente na parte inferior do HUD)

**O que foi feito:** Boss "Rei dos Zumbis" (HP 80, 2× maior, lento) seedado com loot de 15-25 moedas e 60 XP. Fase 4 adicionada ao mapa com 2 ondas de zumbis reforçados + onda final do boss. Ao ativar a onda do boss, o canvas exibe anúncio vermelho "⚔ BOSS — Rei dos Zumbis". Boss desenhado 2× maior com base alinhada ao chão. Enquanto boss está vivo, barra de HP vermelha com gradiente aparece na parte inferior do canvas com nome e HP numérico.

**Critério de aceite:** ✅ `npx tsc --noEmit` sem erros. Seed concluído. Fase 4 desbloqueável no mapa. Boss 2× maior no canvas. Boss HP bar aparece/some corretamente. Anúncio vermelho ao ativar onda do boss.
**Status:** ✅ Concluído

---

## [claude - Victor Fausto] 06/06/2026 — FASE 6 árvore de habilidades global

**Comando:** /dev
**Complexidade:** 🔴 Grande
**Arquivos tocados:**
- prisma/schema.prisma (modificado — models SkillNode e PlayerSkillUnlock; relação skillUnlocks no Player)
- prisma/seed.ts (modificado — 8 nós seedados com IDs fixos: Vitalidade I/II, Força I/II, Resiliência I, Cobiça, Sabedoria, Iluminação)
- src/app/api/skills/route.ts (criado — GET lista de nós com `unlocked` por player)
- src/app/api/skills/unlock/route.ts (criado — POST compra com validação de coins, transação atômica)
- src/app/api/skills/bonus/route.ts (criado — GET bônus agregado de nós desbloqueados)
- src/app/skills/page.tsx (criado — grid de habilidades com compra, saldo de moedas, estados desbloqueado/insuficiente)
- src/components/GameNavbar.tsx (modificado — link SKILLS com ícone estrela)
- src/components/GameCanvas.tsx (modificado — loadGroupData retorna `{slots, bonus}`; aplica allHp/allAttack em cada herói; passa coinBonus ao Game e xpBonus ao onKill endpoint)
- src/game/Game.ts (modificado — construtor aceita `coinBonus`; dropCoins multiplica moedas por `1 + coinBonus`)
- src/app/api/combat/kill/route.ts (modificado — aceita `xpBonus` no body; XP multiplicado por `1 + xpBonus`)

**O que foi feito:** Sistema completo de árvore de habilidades globais. 8 nós compráveis com moedas (HP, ATK, DEF flat + coinBonus e xpBonus percentuais) seedados no banco. Página /skills mostra grid com saldo atual, botões de compra (desabilitados se insuficiente), badge DESBLOQUEADO após compra. Bônus aplicados: allHp/allAttack injetados nos HeroSlots antes de criar os Players; coinBonus multiplica moedas droppadas no Game; xpBonus multiplicado no /api/combat/kill. Link SKILLS adicionado à GameNavbar.

**Critério de aceite:** ✅ `npx tsc --noEmit` passou sem erros. Seed concluído com 8 nós. APIs criadas. Página /skills funcional. Bônus aplicados a HP, ATK, coins e XP.
**Status:** ✅ Concluído

---

## [claude - Victor Fausto] 06/06/2026 — FASE 5 atributos de combate data-driven

**Comando:** /dev
**Complexidade:** 🟡 Média
**Arquivos tocados:**
- src/game/entities/Player.ts (modificado — attackRange como campo readonly recebido no construtor em px, removido get hardcoded 110)
- src/game/entities/Enemy.ts (modificado — attackCooldownMs e attackRangePx como parâmetros; MELEE_RANGE/ATTACK_COOLDOWN removidos; usa this.attackRangePx e this.attackCooldownMs)
- src/game/Game.ts (modificado — HeroSlot.attackRange adicionado; GRID_SIZE=100 constante; Player criado com s.attackRange * GRID_SIZE; autoMove usa p.attackRange; Enemy do DB recebe attackCooldown*1000 e attackRange)
- src/components/GameCanvas.tsx (modificado — attackRange incluído no HeroSlot retornado por loadGroupData)

**O que foi feito:** Arqueiro (attackRange=3 no DB) agora para a 300px dos inimigos e ataca sem avançar mais; Guerreiro (range=1) avança até 100px. Monstros usam o attackCooldown configurado no admin (ex: Zumbi 2.2s). Inimigos com range>1 configurados no admin também respeitarão o valor. GRID_SIZE=100 como constante unificada em Game.ts e Enemy.ts.

**Critério de aceite:** ✅ TypeScript passa sem erros. Arqueiro `attackRange=3` → 300px range confirmado pelo DB (query retornou corretamente). Guerreiro `attackRange=1` → 100px. Zumbi `attackCooldown=2.2` → 2200ms no construtor Enemy.
**Status:** ✅ Concluído

## [claude - Victor Fausto] 06/06/2026 — FASE 10
**Comando:** /dev
**Complexidade:** 🟡 Média
**Arquivos tocados:**
- prisma/seed.ts (modificado — hero-cleric adicionado)
- src/game/Game.ts (modificado — POSITION_DMG_MULT, tickEnemies multi-hero, resolveCombat cleric, HUD labels)

**O que foi feito:** Implementada formação tática com redução de dano por posição (frente 100%, meio 60%, fundo 30%); heróis mortos são ignorados no cálculo. Adicionado Clérigo (`heroClass='cleric'`, 200 moedas) que cura o aliado vivo mais machucado dentro do `attackRange` a cada cooldown, emitindo float verde `+5 HP`. HUD agora exibe labels "FRENTE/MEIO/FUNDO" em cores distintas quando há 2+ heróis no grupo. BADGE_H aumentado de 30 para 36px.

**Critério de aceite:** ✅ `npx tsc --noEmit` passou sem erros. Seed concluído (hero-cleric inserido por 200 moedas). Dano por posição: `Math.round(dmg * [1.0, 0.6, 0.3][i])`, ignorando valores 0 (baixo dano no fundo). Clérigo chama `markHitDealt()` mesmo sem alvo, evitando spam por frame.
**Status:** ✅ Concluído
